-# = provide(:title, 'First Page')

.bg-red-200.w-full
  - if current_user.present? && current_user.stories.present?
    = render current_user.stories.last
  %div
    '------------------------------------------------------------------------------------------------------------------------'
  %span.text-secondary
    asdaad
  %div.text-primary
    asdaad
  %div.text-fade
    asdaad
  %div.text-behindfade
    asdaad
