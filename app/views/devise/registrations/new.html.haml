.w-max.m-auto.text-sm.font-black.color.text-secondary
  Use one of the following applications to sign up on Pe Caiet.
.w-56.m-auto.mt-16.text-sm.font-black.text-secondary
  - if devise_mapping.omniauthable?
    - resource_class.omniauth_providers.each do |provider|
      = button_to omniauth_authorize_path(resource_name, provider), data: { turbo: false } do
        .flex.items-center.border.rounded-md.justify-center.p-2.hover:bg-behindfade.gap-1
          .w-7.h-7
            = image_tag "google.png"
          Continue with Google
